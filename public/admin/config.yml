backend:
  name: github
  repo: nokgc333/naokikaneko
  branch: main
  base_url: https://naokikaneko.vercel.app
  auth_endpoint: api/decap-oauth/auth

local_backend: true

media_folder: "public/images/uploads"
public_folder: "/images/uploads"

collections:
  - name: "work"
    label: "Work"
    folder: "content/work"
    create: true
    slug: "{{fields.slug}}"
    fields:
      - { name: "title", label: "Title", widget: "string" }
      - { name: "slug", label: "Slug", widget: "string", hint: "URLに使われます（例: my-project）" }
      - { name: "date", label: "Date", widget: "datetime", date_format: "YYYY-MM-DD", time_format: false }
      - { name: "tags", label: "Tags", widget: "list", required: false, default: [] }
      - { name: "thumbnail", label: "Thumbnail", widget: "image", required: false }
      - { name: "url", label: "URL", widget: "string", required: false }
      - { name: "description", label: "Description", widget: "string" }
      - { name: "body", label: "Body", widget: "markdown" }

  - name: "blog"
    label: "Blog"
    folder: "content/blog"
    create: true
    slug: "{{fields.slug}}"
    extension: "mdx"
    fields:
      - { name: "title", label: "Title", widget: "string" }
      - { name: "slug", label: "Slug", widget: "string", hint: "URLに使われます（例: my-post）" }
      - { name: "date", label: "Date", widget: "datetime", date_format: "YYYY-MM-DD", time_format: false }
      - { name: "tags", label: "Tags", widget: "list", required: false, default: [] }
      - { name: "thumbnail", label: "Thumbnail", widget: "image", required: false }
      - { name: "url", label: "URL", widget: "string", required: false }
      - { name: "description", label: "Description", widget: "string" }
      - { name: "body", label: "Body", widget: "markdown" }
